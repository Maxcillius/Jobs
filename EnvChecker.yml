name: env-verification-job
image: alpine:latest
command: /bin/sh
args: 
  - -c
  - "echo Connecting to $DB_HOST using key $API_KEY"
workdir: /tmp 
timeout_seconds: 20
env:
  API_KEY: "secret-12345"
  DB_HOST: "prod-db.local"
