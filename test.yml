name: run-backend
command: docker
args:
  - run
  - --rm
  - --name
  - backend
  - -p
  - "8080:8080"
  - -e
  - APP_ENV=production
  - node:alpine
workdir: .
timeout_seconds: 300
env:
  DOCKER_BUILDKIT: "1"
